import React from 'react';
import TestingLibrary from '@testing-library/react';
import { GlobalDialog } from '../../';

describe('GlobalDialog', () => {
  beforeEach(() => {
    jest.resetModules();
  });

  test('初期表示', () => {
    // const renderTest = TestingLibrary.render(<GlobalDialog />);
    // expect('<div></div>').toBe(renderTest.baseElement.innerHTML);
  });
});
