import { Meta, Story, Source, Props } from '@storybook/addon-docs';
import { GlobalDialog, useGlobalDialogStateValue } from '../';
import { useState } from 'react';
import { RecoilRoot } from 'recoil';
import { v4 as uuid } from 'uuid';
import { css } from '@emotion/react';

export const uuid1 = uuid();
export const uuid2 = uuid();
export const uuid3 = uuid();
export const uuid4 = uuid();
export const uuid5 = uuid();

export const Component = () => {
  const [uid5, setUid5] = useState();
  return (
    <RecoilRoot>
      <GlobalDialog
        uid={uuid1}
        contentCSS={css`
          width: 400px;
          height: 400px;
        `}
        onClose={() => setUid5(uuid5)}>
        aaaaaa
      </GlobalDialog>
      <GlobalDialog
        uid={uuid2}
        contentCSS={css`
          width: 300px;
          height: 300px;
        `}>
        bbbbbb
      </GlobalDialog>
      <GlobalDialog
        uid={uuid3}
        contentCSS={css`
          width: 200px;
          height: 200px;
        `}>
        ccccc
      </GlobalDialog>
      <GlobalDialog
        uid={uuid4}
        contentCSS={css`
          width: 100px;
          height: 100px;
        `}
        onClose={() => alert('dddddを閉じる')}>
        ddddd
      </GlobalDialog>
      {uid5 && (
        <GlobalDialog
          uid={uuid5}
          contentCSS={css`
            width: 50px;
            height: 50px;
          `}>
          eeeee
        </GlobalDialog>
      )}
    </RecoilRoot>
  );
};

<Meta title="design_system/dialog/global_dialog/default" component={Component} />

# design_system/dialog/global_dialog/default

global_dialog の説明

<Story name="default" args={{}}></Story>

<Source>
  <Component />
</Source>

<Props story="default" />
