import React from 'react';
import { StandardDialog, StandardDialogProps } from '@design_system/src/components';

export const APICallResultDialog: React.FC<StandardDialogProps> = (props) => {
  return <StandardDialog {...props} />;
};
