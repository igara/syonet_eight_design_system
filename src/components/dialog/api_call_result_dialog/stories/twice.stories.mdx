import { Meta, Story, Source, Props } from '@storybook/addon-docs';
import { APICallResultDialog } from '../';
import { css } from '@emotion/react';

export const props = {
  isOpen: true,
  isFirstTime: true,
  onClose: () => alert('props.onClose()が起動'),
  error: 'API の取得に失敗',
};

<Meta
  title="design_system/dialog/api_call_result_dialog/twice"
  component={() => (
    <div>
      <APICallResultDialog
        {...{
          ...props,
          contentCSS: css`
            width: 300px;
            height: 300px;
          `,
          error: 'API の取得に失敗1',
        }}
      />
      <APICallResultDialog
        {...{
          ...props,
          contentCSS: css`
            width: 200px;
            height: 200px;
          `,
          error: 'API の取得に失敗2',
        }}
      />
    </div>
  )}
/>

# design_system/dialog/api_call_result_dialog/twice

ダイアログを重ねて表示する

<Story name="twice"></Story>

<Props story="twice" />
